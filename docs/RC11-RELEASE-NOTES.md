# ChatVox 1.0.0-rc.11

## Simplified username pronunciation

ChatVox no longer exposes manual per-name pronunciation overrides. The app
uses its built-in conservative username pronunciation automatically, so a
streamer can connect Twitch and start reading chat without extra setup.

This release keeps the existing local-only Twitch authorization, queue,
filtering, and Kokoro speech behavior unchanged.
